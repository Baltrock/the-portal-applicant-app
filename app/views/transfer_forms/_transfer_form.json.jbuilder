json.extract! transfer_form, :id, :current_project, :preferred_project, :current_role, :prefered_role, :transfer_reason, :outcome_expectation, :availability, :user_id, :created_at, :updated_at
json.url transfer_form_url(transfer_form, format: :json)
